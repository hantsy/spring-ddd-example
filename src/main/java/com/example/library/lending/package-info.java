/**
 * The lending module depends only on the shared kernel ({@code common}).
 */
@ApplicationModule(allowedDependencies = "common")
package com.example.library.lending;

import org.springframework.modulith.ApplicationModule;
